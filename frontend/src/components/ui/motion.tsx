// The motion vocabulary moved to the shared design system, where both tools
// get the same three components and the same timing tokens. Re-exported from
// its old path so the existing import sites did not all have to change at
// once.
export { FadeIn, Stagger, StaggerItem } from "../../uikit";
