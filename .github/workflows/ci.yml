name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  backend:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: "1.25"
          cache-dependency-path: backend/go.sum
      - name: Vet
        run: go vet ./...
      - name: OpenAPI spec is in sync with the code
        working-directory: .
        run: make docs-check
      - name: Lint
        # golangci-lint v2 (the .golangci.yml is v2 schema). The binary must be
        # built with Go >= the module's target (go 1.25.0), so pin a v2.5.x
        # release built with go1.25; the older v1 the v6 action installed was
        # built with go1.24 and refused to run against a go1.25 module.
        uses: golangci/golangci-lint-action@v8
        with:
          version: v2.5.0
          working-directory: backend
      - name: Test
        run: go test ./...
      - name: Build
        run: go build ./...
      - name: Smoke (end-to-end write-back)
        working-directory: .
        run: |
          git config --global user.name ci
          git config --global user.email ci@example.com
          ./scripts/smoke.sh

  frontend:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: frontend/package-lock.json
      - run: npm ci || npm install
      - name: Lint
        run: npm run lint
      - name: Typecheck & build
        run: npm run build
